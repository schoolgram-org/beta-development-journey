interface Credentials {
    ver: string;
    expiryDate: number;
    accessToken: string;
}
export default class Session {
    ver: string;
    expiryDate: number;
    accessToken: string;
    constructor(credentials: Credentials);
    isValid(): boolean;
    isExpired(): boolean;
}
export {};
