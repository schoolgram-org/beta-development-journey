import NS from "../NetSchoolApi-safe.js";
import ScheduleDay from "../classes/ScheduleDay";
export interface Credentials {
    date?: Date;
    classId?: number;
}
export default function (this: NS, credentials?: Credentials): Promise<ScheduleDay>;
