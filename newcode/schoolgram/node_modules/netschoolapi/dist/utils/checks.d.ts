/**
 * Этот файл содержит функции, необходимые для проверки данных.
 * Они часто очень просты, но они слишком распространены в методах.
 */
import NS from "../NetSchoolApi-safe.js";
/**
 * Проверяет валидность сессии.
 */
export declare function sessionValid(this: NS): Promise<NS & {
    session: NonNullable<NS["session"]>;
    context: NonNullable<NS["context"]>;
}>;
/**
 * Проверяет валидность дат
 */
export declare function dateValid(this: NS, ...dates: Date[]): Date[];
/**
 * Проверяет валидность дат четверти или возвращает дефолтное значение.
 * @param this Класс библиотеки.
 * @param termId ID четверти.
 */
export declare function termDateValid(this: NS, termId: number, startDate?: Date, endDate?: Date): Promise<{
    start: Date;
    end: Date;
}>;
/**
 * Проверяет id пользователя или возвращает дефолтное
 */
export declare function studentIdValid(this: NS, id?: number): import("../classes/Context").Student;
/**
 * Проверяет id класса или возвращает дефолтное
 */
export declare function classIdValid(this: NS, id?: number): import("../classes/Context").Class;
/**
 * Проверяет id четверти или возвращает дефолтное
 */
export declare function termIdValid(this: NS, id?: number): import("../classes/Context").Term;
