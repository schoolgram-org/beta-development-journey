"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.num2str = num2str;
exports.date2str = date2str;
exports.str2date = str2date;
exports.date2JSON = date2JSON;
/**
 * Переводит число `1` в строку "01"
 */
function num2str(num) {
    if (typeof num == "string")
        num = parseInt(num);
    if (num < 10)
        return "0" + num;
    else
        return num.toString();
}
/**
 * Переводит объект `Date` в строку формата "dd.mm.yy"
 */
function date2str(date) {
    const day = date.getDate();
    const month = date.getMonth() + 1;
    const year = date.getFullYear() % 100;
    return day + "." + month + "." + year;
}
/**
 * Переводит строку формата "dd.mm.yy" в объект `Date`
 */
function str2date(str) {
    if (!/(\d{1,2}\.){2}\d{2}/.test(str))
        throw new Error("Invalid date string: " + str);
    const [day, month, year] = str.match(/\d+/g).map((s) => parseInt(s));
    const date = new Date();
    date.setHours(0, 0, 0, 0);
    date.setDate(day);
    date.setMonth(month - 1);
    date.setFullYear(~~(date.getFullYear() / 100) * 100 + year);
    return date;
}
function date2JSON(arg) {
    const date = typeof arg === "string" ? str2date(arg) : arg;
    const day = date.getDate();
    const year = date.getFullYear();
    const month = date.getMonth() + 1;
    return `${year}-${num2str(month)}-${num2str(day)}T00:00`;
}
//# sourceMappingURL=dateNum.js.map