import { Credentials } from "../../../NetSchoolApi-safe.js";
import Client from "../../../classes/Client";
export interface SignIn {
    at: string;
    ver: string;
    timeOut: number;
}
export default function (client: Client, credentials: Credentials): Promise<SignIn>;
