import NS from "../NetSchoolApi-safe.js";
import Diary from "../classes/Diary";
export interface Credentials {
    studentId?: number;
    start?: Date;
    end?: Date;
}
export default function (this: NS, credentials?: Credentials): Promise<Diary>;
