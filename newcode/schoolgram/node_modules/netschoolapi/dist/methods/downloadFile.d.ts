import NS from "../NetSchoolApi-safe.js";
export interface Credentials {
    studentId?: number;
    assignId: number;
    id: number;
}
export default function (this: NS, credentials: Credentials): Promise<Buffer<ArrayBufferLike>>;
